# CORS Configuration
# Specify allowed origins for CORS requests
# For multiple origins, separate them with commas (no spaces around commas)
# Example: CORS_ORIGINS=http://localhost:5173,http://localhost:3000
CORS_ORIGINS=http://localhost:5173
